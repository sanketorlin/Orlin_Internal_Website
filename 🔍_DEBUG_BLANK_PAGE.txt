╔══════════════════════════════════════════════════════════════════════════╗
║                    🔍  DEBUG: BLANK PAGE ISSUE                           ║
╚══════════════════════════════════════════════════════════════════════════╝

To find out why the page is blank, check these:

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 1: Check Browser Console                                            │
└──────────────────────────────────────────────────────────────────────────┘

1. Open your Vercel URL: https://orlin-internal-website.vercel.app
2. Press F12 (open Developer Tools)
3. Click "Console" tab
4. Look for RED error messages

Common errors you might see:

❌ "Failed to load resource" (404 errors)
   → Assets not loading correctly
   → Fix: Check vercel.json configuration

❌ "Firebase: Error (auth/invalid-api-key)"
   → Firebase config is wrong
   → Fix: Check src/firebase/config.js

❌ "Uncaught ReferenceError"
   → JavaScript error in code
   → Fix: Check the error message

❌ "Failed to fetch"
   → Network/CORS issue
   → Fix: Check Firebase CORS settings

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 2: Check Vercel Deployment Logs                                     │
└──────────────────────────────────────────────────────────────────────────┘

1. Go to: https://vercel.com/dashboard
2. Click your project
3. Click on latest deployment
4. Click "Build Logs" or "Function Logs"
5. Look for errors (red text)

Common build errors:

❌ "Build failed"
   → Check build logs for specific error
   → Usually dependency or config issue

❌ "Module not found"
   → Missing dependency
   → Fix: Check package.json

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 3: Test Build Locally                                               │
└──────────────────────────────────────────────────────────────────────────┘

Run these commands:

npm run build
npm run preview

If this works locally but not on Vercel:
→ It's a Vercel configuration issue

If this fails locally:
→ There's a code issue to fix first

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 4: Check Network Tab                                                 │
└──────────────────────────────────────────────────────────────────────────┘

1. Open Developer Tools (F12)
2. Go to "Network" tab
3. Refresh the page
4. Look for failed requests (red)

Check if these files load:
✅ index.html (should be 200)
✅ /assets/index-XXXXX.js (should be 200)
✅ /assets/index-XXXXX.css (should be 200)

If any show 404:
→ Assets not being served correctly
→ Fix: Check vercel.json outputDirectory

╔══════════════════════════════════════════════════════════════════════════╗
║  Share the console errors with me and I'll help fix them!               ║
╚══════════════════════════════════════════════════════════════════════════╝



