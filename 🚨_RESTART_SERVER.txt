╔══════════════════════════════════════════════════════════════════════════╗
║          🚨 RESTART SERVER - Function Not Loaded Yet                     ║
╚══════════════════════════════════════════════════════════════════════════╝

The new function isn't loaded because the dev server needs to restart!

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 1: Restart Dev Server                                               │
└──────────────────────────────────────────────────────────────────────────┘

1. In your terminal/command prompt (where npm run dev is running)
2. Press Ctrl+C to stop the server
3. Wait for it to stop completely
4. Type: npm run dev
5. Press Enter
6. Wait for "Local: http://localhost:3000" message

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 2: Refresh Browser                                                  │
└──────────────────────────────────────────────────────────────────────────┘

1. Go to your website: http://localhost:3000
2. Press Ctrl+Shift+R (hard refresh)
3. Make sure you're logged in

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 3: Try Again                                                       │
└──────────────────────────────────────────────────────────────────────────┘

1. Press F12 → Console tab
2. Type: window.initFirestore()
3. Press Enter
4. Should work now!

┌──────────────────────────────────────────────────────────────────────────┐
│ ALTERNATIVE: Manual Method (If above doesn't work)                      │
└──────────────────────────────────────────────────────────────────────────┘

If window.initFirestore() still doesn't work, use this:

1. On Laptop 1: Open console (F12)
2. Get your current reports:
   
   JSON.parse(localStorage.getItem('bi-reports'))

3. Copy the output (the array of reports)
4. Go to Firebase Console → Firestore → Data
5. Click "+ Start collection"
6. Collection ID: reports
7. Click "Next"
8. Document ID: all-reports
9. Click "Add field"
10. Field name: reports
11. Type: array
12. Click "Add item" for each report, or paste the array
13. Click "Save"

╔══════════════════════════════════════════════════════════════════════════╗
║  DO THIS: Restart server (Ctrl+C, then npm run dev), then try again!    ║
╚══════════════════════════════════════════════════════════════════════════╝



