╔══════════════════════════════════════════════════════════════════════════╗
║              🔍 DEBUG: Why Sync Isn't Working                            ║
╚══════════════════════════════════════════════════════════════════════════╝

Let's find out exactly what's wrong!

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 1: Check Firestore Connection (On BOTH Laptops)                     │
└──────────────────────────────────────────────────────────────────────────┘

1. Open your website
2. Press F12 → Console tab
3. Type this and press Enter:

   window.debugSync.full()

4. Look at the output - it will tell you:
   ✅ If Firestore is connected
   ✅ How many reports are in Firestore
   ✅ How many reports are in localStorage
   ✅ What the problem is

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 2: Check What's in Firestore                                       │
└──────────────────────────────────────────────────────────────────────────┘

1. Go to: https://console.firebase.google.com/
2. Select your project
3. Click "Firestore Database" → "Data" tab
4. Check if "reports" collection exists
5. Click on "reports" → "all-reports"
6. See what reports are stored there

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 3: Common Issues & Fixes                                           │
└──────────────────────────────────────────────────────────────────────────┘

ISSUE 1: "Firestore not connected"
───────────────────────────────────
Fix:
1. Make sure Firestore is enabled in Firebase Console
2. Check security rules allow authenticated users
3. Make sure you're logged in

ISSUE 2: "Firestore is empty"
──────────────────────────────
Fix:
1. On Laptop 1: Delete a report
2. Check console - should see "Saved reports to Firestore"
3. If not, there's an error - check console for details

ISSUE 3: "Firestore has old data"
─────────────────────────────────
Fix:
1. In Firebase Console → Firestore → Data
2. Delete the "reports" collection
3. Refresh your website
4. Delete reports again (they'll save to Firestore)

ISSUE 4: "Laptop 2 shows old code"
───────────────────────────────────
Fix:
1. Make sure Laptop 2 has updated files:
   - src/utils/reportsService.js
   - src/components/Dashboard.jsx
   - src/utils/debugSync.js (new file)

2. Restart dev server on Laptop 2:
   - Ctrl+C to stop
   - npm run dev to start

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 4: Force Sync from Laptop 1                                       │
└──────────────────────────────────────────────────────────────────────────┘

On Laptop 1 (where you deleted reports):

1. Open console (F12)
2. Type:

   window.debugSync.firestore()

3. Check how many reports are in Firestore
4. If it shows 2 reports (Sales + Finance), that's correct!
5. If it shows 4 reports (with HR + Marketing), Firestore has old data

To fix old data:
1. In Firebase Console → Firestore → Data
2. Delete "reports" collection
3. On Laptop 1: Refresh page
4. Delete HR and Marketing reports again
5. Check Firestore - should now show 2 reports

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 5: Check Real-Time Listener on Laptop 2                            │
└──────────────────────────────────────────────────────────────────────────┘

On Laptop 2:

1. Open console (F12)
2. Type:

   window.debugSync.full()

3. Check if it says "Firestore connected: true"
4. If false, Firestore isn't working on Laptop 2
5. If true, wait 5 seconds after deleting on Laptop 1
6. Check console - should see "Real-time update received"

╔══════════════════════════════════════════════════════════════════════════╗
║  DO THIS NOW: Run window.debugSync.full() on BOTH laptops                ║
║  Then tell me what it says!                                              ║
╚══════════════════════════════════════════════════════════════════════════╝

