╔══════════════════════════════════════════════════════════════════════════╗
║          QUICK FIX: Initialize Firestore (No Restart Needed)             ║
╚══════════════════════════════════════════════════════════════════════════╝

Since window.initFirestore() isn't working yet, use this DIRECT method:

┌──────────────────────────────────────────────────────────────────────────┐
│ METHOD 1: Using Import (Easiest)                                        │
└──────────────────────────────────────────────────────────────────────────┘

1. Open console (F12)
2. Type this EXACTLY (copy-paste):

import('./src/utils/reportsService.js').then(async (module) => {
  const local = localStorage.getItem('bi-reports');
  let reports = [];
  if (local) {
    reports = JSON.parse(local);
  } else {
    reports = [
      {id: "sales-report", title: "Sales Report", description: "Comprehensive sales analytics and performance metrics", url: "https://app.powerbi.com/view?r=...", iframe: true, allowedRoles: ["team-head", "sales"], icon: "📊", category: "Sales"},
      {id: "finance-report", title: "Finance Report", description: "Financial overview and budget analysis", url: "https://example.com/finance", iframe: true, allowedRoles: ["team-head", "finance"], icon: "💰", category: "Finance"}
    ];
  }
  await module.saveReports(reports);
  console.log('✅ Firestore initialized with', reports.length, 'reports');
});

3. Press Enter
4. Wait for "✅ Firestore initialized" message

┌──────────────────────────────────────────────────────────────────────────┐
│ METHOD 2: Simple Direct Save                                            │
└──────────────────────────────────────────────────────────────────────────┘

1. Open console (F12)
2. Get your current reports:
   
   const reports = JSON.parse(localStorage.getItem('bi-reports'));

3. Then save to Firestore:
   
   import('./src/utils/reportsService.js').then(async (m) => {
     await m.saveReports(reports);
     console.log('✅ Saved to Firestore!');
   });

╔══════════════════════════════════════════════════════════════════════════╗
║  Try METHOD 1 first - it's the easiest!                                  ║
╚══════════════════════════════════════════════════════════════════════════╝



