╔══════════════════════════════════════════════════════════════════════════╗
║          📋 HOW TO REVIEW CHANGES BEFORE PUSHING                           ║
╚══════════════════════════════════════════════════════════════════════════╝

Here are all the ways to check your changes before pushing to GitHub:

┌──────────────────────────────────────────────────────────────────────────┐
│ METHOD 1: Check What Files Changed (Quick Overview)                      │
└──────────────────────────────────────────────────────────────────────────┘

In terminal, type:
  git status

This shows:
✅ Files that were modified
✅ Files that were added (new)
✅ Files that were deleted

┌──────────────────────────────────────────────────────────────────────────┐
│ METHOD 2: See Summary of Changes (How Many Lines Changed)                │
└──────────────────────────────────────────────────────────────────────────┘

In terminal, type:
  git diff --stat

This shows:
- Which files changed
- How many lines were added (+)
- How many lines were removed (-)

Example output:
  src/components/UserManager.jsx | 150 +++++++++ 50 ----
  src/utils/usersService.js      | 200 +++++++++++++++++

┌──────────────────────────────────────────────────────────────────────────┐
│ METHOD 3: See Actual Code Changes (Detailed View)                        │
└──────────────────────────────────────────────────────────────────────────┘

To see changes in a specific file:
  git diff src/components/UserManager.jsx

To see ALL changes:
  git diff

This shows:
- Lines in RED (with -) = Removed
- Lines in GREEN (with +) = Added
- Context around changes

┌──────────────────────────────────────────────────────────────────────────┐
│ METHOD 4: See Changes in VS Code (Visual)                                 │
└──────────────────────────────────────────────────────────────────────────┘

1. Open VS Code
2. Click "Source Control" icon (left sidebar) or press Ctrl+Shift+G
3. You'll see:
   - Modified files (M)
   - New files (U)
   - Deleted files (D)
4. Click any file to see changes side-by-side
5. Green = Added, Red = Removed

┌──────────────────────────────────────────────────────────────────────────┐
│ METHOD 5: See What Will Be Committed                                      │
└──────────────────────────────────────────────────────────────────────────┘

After running "git add .", check what's staged:
  git diff --cached

This shows only changes that will be committed.

┌──────────────────────────────────────────────────────────────────────────┐
│ METHOD 6: Compare with Last Commit                                        │
└──────────────────────────────────────────────────────────────────────────┘

See what changed since last commit:
  git diff HEAD

See summary:
  git diff HEAD --stat

┌──────────────────────────────────────────────────────────────────────────┐
│ QUICK COMMANDS SUMMARY                                                    │
└──────────────────────────────────────────────────────────────────────────┘

git status              → List all changed files
git diff --stat         → Summary of changes (lines added/removed)
git diff                → See all code changes
git diff <filename>     → See changes in specific file
git diff --cached       → See staged changes (after git add)
git diff HEAD           → Compare with last commit

┌──────────────────────────────────────────────────────────────────────────┐
│ RECOMMENDED WORKFLOW                                                      │
└──────────────────────────────────────────────────────────────────────────┘

1. Check what changed:
   git status

2. See summary:
   git diff --stat

3. Review important files:
   git diff src/components/UserManager.jsx
   git diff src/utils/usersService.js

4. If everything looks good:
   git add .
   git commit -m "Your message"
   git push

╔══════════════════════════════════════════════════════════════════════════╗
║  TIP: Use "git status" first, then "git diff --stat" for quick review!  ║
╚══════════════════════════════════════════════════════════════════════════╝



